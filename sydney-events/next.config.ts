import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
  domains: ['secure.meetupstatic.com', 'img.meetup.com', 'photos.meetupstatic.com', "photon.meetupstatic.com"],
}

};

export default nextConfig;
